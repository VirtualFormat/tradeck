fastapi>=0.115
uvicorn[standard]>=0.30
asyncpg>=0.30
APScheduler>=3.10
httpx>=0.27
python-dateutil>=2.9
pydantic>=2.9
# 板块/自定义模型数据（OpenBB REST 只暴露标准模型，ConceptBoards 等自定义模型需直接调 akshare）
akshare>=1.12
# 财报日历（OpenBB /equity/calendar/earnings 仅 fmp 付费源可用，免费方案为 backend 直调 yfinance 库）
yfinance>=0.2
# 技术指标本地计算（原由 akshare 间接带入，显式声明）
pandas>=3.0
# 日K 稳定源（官方 SDK，自带分片+并发闸+429退避）；免费档 TickFlow.free()
tickflow[all]>=0.1
# 冷层存储（分钟K/tick Parquet）：pyarrow 写列存、boto3 走 S3 兼容（COS）
pyarrow>=15.0
boto3>=1.34
# findb COS 全量包：zstandard 解压 *.zst，DuckDB 只读校验/抽取 core 元数据。
zstandard>=0.23
duckdb>=1.5,<2
