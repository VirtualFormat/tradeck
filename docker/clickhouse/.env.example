# ClickHouse 温层（分钟K 在线 1 年窗口）
# collector 双写 / data-api 读取的 HTTP 端点；容器网络内服务名直达。
# dev 与 prod 的 compose 均已内置默认值 http://clickhouse:8123，一般无需覆盖；
# 仅在 CH 部署到其他主机/端口时在此设置。
# CLICKHOUSE_URL=http://clickhouse:8123
#
# 安全说明：单节点不设密码，仅容器网络内可达（与 postgres 开发默认值同级）。
# prod compose 不映射宿主机端口；若确需公网暴露，必须自行加固
# （CLICKHOUSE_USER / CLICKHOUSE_PASSWORD + 网络层 ACL）。
